name: JS Actions

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  action:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v2
      - name: Install Node modules
        uses: actions/setup-node@v2
      - run: npm --prefix ./psi install ./psi
      - name: Use local PSI
        uses: ./psi